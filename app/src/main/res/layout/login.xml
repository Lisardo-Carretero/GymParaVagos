<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".LoginPage" tools:ignore="LabelFor" android:backgroundTint="#FFFFFF">
    <LinearLayout
            android:elevation="4dp"
            android:padding="16dp"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" android:id="@+id/container_login"
    >
        <EditText style="@style/Widget.AppCompat.AutoCompleteTextView"
                  android:id="@+id/textEdit_user"
                  android:layout_height="wrap_content"
                  android:layout_width="match_parent"
                  android:inputType="textPersonName"
                  android:hint="@string/Usuario"/>
        <EditText
                android:id="@+id/textEdit_pass"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:inputType="textPassword"
                android:hint="@string/Password"
                android:layout_marginTop="12dp"
                style="@style/Widget.AppCompat.EditText"/>
        <LinearLayout
                android:gravity="center"
                android:layout_gravity="center"
                android:orientation="horizontal"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" android:id="@+id/bt_layout">

            <ImageButton

                    android:id="@+id/bt_google"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    app:srcCompat="@drawable/common_google_signin_btn_icon_dark"
                    android:layout_weight="1"
                    android:background="@null" android:contentDescription="Inciar con google"
            />
            <ImageButton
                    android:id="@+id/bt_login"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    app:srcCompat="@drawable/ic_login"
                    android:layout_weight="1"
                    android:contentDescription="Inciar con google"

                    android:backgroundTint="#FAFAFA"/>

        </LinearLayout>

    </LinearLayout>

    <TextView
            android:layout_margin="24dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/Gym"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:id="@+id/textView" android:textSize="24sp"
            android:textAppearance="@style/TextAppearance.AppCompat.Large" android:textStyle="bold"
            android:fontFamily="monospace"/>


</androidx.constraintlayout.widget.ConstraintLayout>